machine:
  services:
    - docker
dependencies:
  override:
    - sh -e build.sh
deployment:
  master:
    branch: master
    commands:
      - IMAGE_TAG=latest sh -e deploy.sh
  development:
    branch: development
    commands:
      - IMAGE_TAG=dev sh -e deploy.sh
